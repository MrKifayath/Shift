{
  "name": "@pomodoro-app/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for Pomodoro Productivity App",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "typescript": "^5.1.3",
    "rimraf": "^5.0.5"
  },
  "dependencies": {
    "class-validator": "^0.14.0",
    "class-transformer": "^0.5.1"
  }
}